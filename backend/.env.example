# Example database URL (do NOT commit secrets)
# Use one of these environment variable names in your shell or process:
# DATABASE_URL or POSTGRES_DSN
# Example (Postgres connection string):
DATABASE_URL=postgresql://username:password@localhost:5432/mydb?sslmode=disable

# For local development you can set this in PowerShell:
# $env:DATABASE_URL = "postgresql://username:password@localhost:5432/mydb?sslmode=disable"
